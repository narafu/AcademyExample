window.addEventListener("load", function () {

    // var visual = document.querySelector("#visual");
    // var arrowLeft = visual.querySelector(".fa-arrow-circle-left");
    // var arrowRight = visual.querySelector(".fa-arrow-circle-right");
    // var slide = visual.querySelector(".slide");
    // var activeImg = slide.querySelector(".active");
    // var slideImg = slide.querySelectorAll(".hidden img");
    // var circle = visual.querySelector(".circle");
    // var circleBtn = circle.querySelectorAll("i");
    // var index = 0;
    // activeImg.src = slideImg[0].src;    

    // arrowLeft.onclick = function (e) {

    //     var current = (slideImg.length - (index % slideImg.length)) % slideImg.length;
    //     circleBtn[current].classList.remove("fas");
    //     circleBtn[current].classList.add("far");

    //     var next = (slideImg.length - (++index % slideImg.length)) % slideImg.length;
        
    //     activeImg.src = slideImg[next].src;
    //     circleBtn[next].classList.remove("far");
    //     circleBtn[next].classList.add("fas");

    // }

    // arrowRight.onclick = function (e) {

    //     var current = index % slideImg.length;
    //     circleBtn[current].classList.remove("fas");
    //     circleBtn[current].classList.add("far");

    //     var next = ++index % slideImg.length;

    //     activeImg.src = slideImg[next].src;
    //     circleBtn[next].classList.remove("far");
    //     circleBtn[next].classList.add("fas");
    // }

})